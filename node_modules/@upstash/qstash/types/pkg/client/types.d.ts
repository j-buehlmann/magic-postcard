export declare type State = "created" | "planned" | "active" | "delivered" | "error" | "failed" | "canceled";
export declare type Log = {
    time: number;
    state: State;
    messageId: string;
    taskId?: string;
    nextScheduledAt?: number;
    error?: string;
};
export declare type WithCursor<T> = T & {
    cursor?: number;
};
export declare type Task = {
    taskId: string;
    state: State;
    maxRetry: number;
    retried: number;
    completedAt?: number;
    url: string;
};
